server:
  port: 8080
spring:
  application: 
    name: zuul-gateway
  cloud:
    config:
      uri: http://localhost:8888/
            
management:
  endpoints:
    web:
      exposure:
        include: "*"