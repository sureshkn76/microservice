server:
  port: 8081
spring:
  application: 
    name: zuul-gateway-jwt
  cloud:
    config:
      uri: http://localhost:8888/
            
management:
  endpoints:
    web:
      exposure:
        include: "*"