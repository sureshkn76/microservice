server:
  port: 8888 
spring:
  application: 
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
##        search-locations: classpath:/config
          search-locations: file:///C:/JavaWorkspace/config-server/src/main/resources/
        bootstrap: true   